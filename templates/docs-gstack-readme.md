# Gstack Workflow Artifacts

This directory stores tracked workflow outputs for the Codex-first gstack process.

- One initiative per subdirectory
- `brief.md` comes from `/office-hours`
- `plan.md` comes from `/autoplan`
- `retro.md` comes from `/retro`

Machine-only workflow state lives under `.codex-gstack/workflow/`.
