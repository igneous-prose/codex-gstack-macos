# Notice

`codex-gstack-macos` is an original rebuild informed by workflow ideas from `garrytan/gstack`.

Inspirations retained at a high level:

- skill-oriented workflow shape
- localhost browser command surface with token auth
- strict output-path validation
- explicit cookie import as an advanced local action

This repository is not a fork and does not carry upstream host integrations, telemetry, tunnel flows, or global hook behavior.

